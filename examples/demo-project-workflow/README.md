# Demo Project Workflow

This is a complete example run for the factory skill. It shows the expected
artifact set, confirmation record, double-review report, validator evidence and
generated reusable workflow skill.

Validate it from the repository root:

```bash
npm run validate
```

The example intentionally treats the workflow validator as the build/test
command because this repository is a skill template, not an application codebase.
